// Generated by ModelLang. Do not edit.

export type ApplicabilityStatus = "applicable" | "denied" | "notApplicable" | "stale";
export type ApplicabilityExplanationKind = "authorization" | "requirement" | "revision";
export interface ApplicabilityDecision {
  readonly operationId: string;
  readonly status: ApplicabilityStatus;
  readonly applicable: boolean;
  readonly authority: "none";
  readonly revision?: string;
  readonly explanation?: { readonly kind: ApplicabilityExplanationKind; readonly ruleId: string };
}
export interface ExecutionOptions {
  readonly expectedRevision?: string;
  readonly idempotencyKey?: string;
  readonly correlationId?: string;
  readonly causationId?: string;
}
export interface ApplicabilityOptions { readonly expectedRevision?: string; }
export interface CursorPage<T> {
  readonly items: T[];
  readonly nextCursor: string | null;
}

export type PrincipalKind = "HUMAN" | "AGENT";

export type PrincipalStatus = "ACTIVE" | "REVOKED";

export type Role = "MEMBER" | "APPROVER" | "ADMIN";

export type EnvironmentTier = "STAGING" | "PRODUCTION";

export type Capability = "DEPLOY_STAGING" | "REQUEST_PRODUCTION_DEPLOY";

export type DelegationStatus = "ACTIVE" | "REVOKED";

export type RequestStatus = "PENDING_APPROVAL" | "APPROVED" | "REJECTED" | "EXECUTED";

export interface Organization {
  /** Generated by the database using uuid; immutable after creation. */
  id: string;
  /** Immutable after creation. */
  slug: string;
  name: string;
}

export interface Principal {
  /** Generated by the database using uuid; immutable after creation. */
  id: string;
  /** Immutable after creation. */
  org: string;
  /** Immutable after creation. */
  kind: PrincipalKind;
  displayName: string;
  status: PrincipalStatus;
  roles: Role[];
}

export interface Environment {
  /** Generated by the database using uuid; immutable after creation. */
  id: string;
  /** Immutable after creation. */
  org: string;
  name: string;
  /** Immutable after creation. */
  tier: EnvironmentTier;
}

export interface Delegation {
  /** Generated by the database using uuid; immutable after creation. */
  id: string;
  /** Immutable after creation. */
  org: string;
  /** Immutable after creation. */
  agent: string;
  /** Immutable after creation. */
  capability: Capability;
  /** Immutable after creation. */
  environment: string;
  status: DelegationStatus;
}

export interface Allowance {
  /** Generated by the database using uuid; immutable after creation. */
  id: string;
  /** Immutable after creation. */
  org: string;
  /** Immutable after creation. */
  period: string;
  /** Immutable after creation. */
  sequence: number;
}

export interface ProductionDeployRequest {
  /** Generated by the database using uuid; immutable after creation. */
  id: string;
  /** Generated by the database using now; immutable after creation. */
  createdAt: string;
  /** Immutable after creation. */
  org: string;
  /** Immutable after creation. */
  requestedBy: string;
  /** Immutable after creation. */
  environment: string;
  /** Immutable after creation. */
  commitSha: string;
  status: RequestStatus;
  approvedBy: string | null;
  /** Stored point-in-time snapshot; it is not re-derived from a related entity. */
  approvedByRoles: Role[] | null;
}

export interface Approval {
  /** Generated by the database using uuid; immutable after creation. */
  id: string;
  /** Generated by the database using now; immutable after creation. */
  decidedAt: string;
  /** Immutable after creation. */
  org: string;
  request: string;
  /** Immutable after creation. */
  approver: string;
}

export interface Execution {
  /** Generated by the database using uuid; immutable after creation. */
  id: string;
  /** Generated by the database using now; immutable after creation. */
  startedAt: string;
  /** Immutable after creation. */
  org: string;
  request: string;
  allowance: string;
}

export interface PrincipalSummary {
  id: string;
  displayName: string;
  kind: PrincipalKind;
}

export interface DeployRequestSummary {
  id: string;
  createdAt: string;
  commitSha: string;
  status: RequestStatus;
  requestedBy: PrincipalSummary;
  approvedBy: PrincipalSummary | null;
}

export interface RequestProductionDeploymentInput {
  delegation: string;
  environment: string;
  commitSha: string;
}

export interface ApproveProductionDeploymentInput {
  request: string;
}

export interface ExecuteApprovedDeploymentInput {
  request: string;
  allowance: string;
}

export interface RejectProductionDeploymentInput {
  request: string;
}

export interface MyOrgDeployRequestsInput {
  cursor?: string;
}

export interface ApprovalInboxInput {
  cursor?: string;
}

