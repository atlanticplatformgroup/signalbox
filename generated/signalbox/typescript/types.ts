// Generated by ModelLang. Do not edit.

export type ApplicabilityStatus = "applicable" | "denied" | "notApplicable" | "stale";
export type ApplicabilityExplanationKind = "authorization" | "requirement" | "revision";
export interface ApplicabilityDecision {
  readonly operationId: string;
  readonly status: ApplicabilityStatus;
  readonly applicable: boolean;
  readonly authority: "none";
  readonly revision?: string;
  readonly explanation?: { readonly kind: ApplicabilityExplanationKind; readonly ruleId: string };
}
export interface ExecutionOptions {
  readonly expectedRevision?: string;
  readonly idempotencyKey?: string;
  readonly correlationId?: string;
  readonly causationId?: string;
}
export interface ApplicabilityOptions { readonly expectedRevision?: string; }
export interface CursorPage<T> {
  readonly items: T[];
  readonly nextCursor: string | null;
}

export type PrincipalKind = "HUMAN" | "AGENT";

export type PrincipalStatus = "ACTIVE" | "REVOKED";

export type Role = "MEMBER" | "APPROVER" | "ADMIN" | "EXECUTOR";

export type ConnectorKind = "GITHUB" | "STATIC_SITE" | "POSTGRESQL";

export type ConnectorStatus = "ACTIVE" | "REVOKED";

export type EnvironmentTier = "STAGING" | "PRODUCTION";

export type Capability = "CREATE_ISSUE" | "OPEN_PULL_REQUEST" | "DEPLOY_STAGING" | "REQUEST_PRODUCTION_DEPLOY" | "REQUEST_SCHEMA_MIGRATION";

export type DelegationStatus = "ACTIVE" | "REVOKED";

export type RequestStatus = "READY" | "PENDING_APPROVAL" | "APPROVED" | "REJECTED" | "DISPATCHED";

export type RequestKind = "ISSUE" | "PULL_REQUEST" | "DEPLOYMENT" | "SCHEMA_MIGRATION";

export type ExecutionStatus = "PENDING" | "SUCCEEDED" | "FAILED";

export interface Organization {
  /** Generated by the database using uuid; immutable after creation. */
  id: string;
  /** Immutable after creation. */
  slug: string;
  name: string;
}

export interface Principal {
  /** Generated by the database using uuid; immutable after creation. */
  id: string;
  /** Immutable after creation. */
  org: string;
  /** Immutable after creation. */
  kind: PrincipalKind;
  displayName: string;
  status: PrincipalStatus;
  roles: Role[];
  /** Immutable after creation. */
  responsibleOwner: string | null;
}

export interface AgentCredentialMetadata {
  /** Generated by the database using uuid; immutable after creation. */
  id: string;
  /** Immutable after creation. */
  org: string;
  /** Immutable after creation. */
  agent: string;
  /** Immutable after creation. */
  label: string;
  /** Immutable after creation. */
  tokenHash: string;
  /** Immutable after creation. */
  expiresAt: string | null;
  revokedAt: string | null;
  lastUsedAt: string | null;
}

export interface Connector {
  /** Generated by the database using uuid; immutable after creation. */
  id: string;
  /** Immutable after creation. */
  org: string;
  name: string;
  /** Immutable after creation. */
  kind: ConnectorKind;
  status: ConnectorStatus;
}

export interface Repository {
  /** Generated by the database using uuid; immutable after creation. */
  id: string;
  /** Immutable after creation. */
  org: string;
  /** Immutable after creation. */
  connector: string;
  /** Immutable after creation. */
  owner: string;
  /** Immutable after creation. */
  name: string;
  defaultBranch: string;
}

export interface Environment {
  /** Generated by the database using uuid; immutable after creation. */
  id: string;
  /** Immutable after creation. */
  org: string;
  /** Immutable after creation. */
  repository: string;
  /** Immutable after creation. */
  connector: string;
  name: string;
  /** Immutable after creation. */
  tier: EnvironmentTier;
}

export interface Delegation {
  /** Generated by the database using uuid; immutable after creation. */
  id: string;
  /** Immutable after creation. */
  org: string;
  /** Immutable after creation. */
  agent: string;
  /** Immutable after creation. */
  capability: Capability;
  /** Immutable after creation. */
  repository: string | null;
  /** Immutable after creation. */
  environment: string | null;
  /** Immutable after creation. */
  connector: string;
  status: DelegationStatus;
}

export interface Allowance {
  /** Generated by the database using uuid; immutable after creation. */
  id: string;
  /** Immutable after creation. */
  org: string;
  /** Immutable after creation. */
  agent: string;
  /** Immutable after creation. */
  period: string;
  /** Immutable after creation. */
  sequence: number;
}

export interface IssueRequest {
  /** Generated by the database using uuid; immutable after creation. */
  id: string;
  /** Generated by the database using now; immutable after creation. */
  createdAt: string;
  /** Immutable after creation. */
  org: string;
  /** Immutable after creation. */
  requestedBy: string;
  /** Immutable after creation. */
  delegation: string;
  /** Immutable after creation. */
  repository: string;
  /** Immutable after creation. */
  connector: string;
  /** Immutable after creation. */
  title: string;
  /** Immutable after creation. */
  body: string;
  status: RequestStatus;
}

export interface PullRequest {
  /** Generated by the database using uuid; immutable after creation. */
  id: string;
  /** Generated by the database using now; immutable after creation. */
  createdAt: string;
  /** Immutable after creation. */
  org: string;
  /** Immutable after creation. */
  requestedBy: string;
  /** Immutable after creation. */
  delegation: string;
  /** Immutable after creation. */
  repository: string;
  /** Immutable after creation. */
  connector: string;
  /** Immutable after creation. */
  headBranch: string;
  /** Immutable after creation. */
  baseBranch: string;
  /** Immutable after creation. */
  title: string;
  status: RequestStatus;
}

export interface DeploymentRequest {
  /** Generated by the database using uuid; immutable after creation. */
  id: string;
  /** Generated by the database using now; immutable after creation. */
  createdAt: string;
  /** Immutable after creation. */
  org: string;
  /** Immutable after creation. */
  requestedBy: string;
  /** Immutable after creation. */
  delegation: string;
  /** Immutable after creation. */
  environment: string;
  /** Stored point-in-time snapshot; it is not re-derived from a related entity. */
  environmentTier: EnvironmentTier;
  /** Immutable after creation. */
  connector: string;
  /** Immutable after creation. */
  commitSha: string;
  status: RequestStatus;
  approvedBy: string | null;
  /** Stored point-in-time snapshot; it is not re-derived from a related entity. */
  approvedByRoles: Role[] | null;
}

export interface SchemaMigrationRequest {
  /** Generated by the database using uuid; immutable after creation. */
  id: string;
  /** Generated by the database using now; immutable after creation. */
  createdAt: string;
  /** Immutable after creation. */
  org: string;
  /** Immutable after creation. */
  requestedBy: string;
  /** Immutable after creation. */
  delegation: string;
  /** Immutable after creation. */
  environment: string;
  /** Immutable after creation. */
  connector: string;
  /** Immutable after creation. */
  migrationName: string;
  /** Immutable after creation. */
  migrationSha: string;
  status: RequestStatus;
  approvedBy: string | null;
  /** Stored point-in-time snapshot; it is not re-derived from a related entity. */
  approvedByRoles: Role[] | null;
}

export interface Approval {
  /** Generated by the database using uuid; immutable after creation. */
  id: string;
  /** Generated by the database using now; immutable after creation. */
  decidedAt: string;
  /** Immutable after creation. */
  org: string;
  requestId: string;
  /** Immutable after creation. */
  requestKind: RequestKind;
  /** Immutable after creation. */
  requestedBy: string;
  /** Immutable after creation. */
  approver: string;
  /** Stored point-in-time snapshot; it is not re-derived from a related entity. */
  approverRoles: Role[];
}

export interface Execution {
  /** Generated by the database using uuid; immutable after creation. */
  id: string;
  /** Generated by the database using now; immutable after creation. */
  startedAt: string;
  /** Immutable after creation. */
  org: string;
  requestId: string;
  /** Immutable after creation. */
  requestKind: RequestKind;
  /** Immutable after creation. */
  requestedBy: string;
  /** Immutable after creation. */
  connector: string;
  allowance: string;
  status: ExecutionStatus;
  externalReference: string | null;
  failureMessage: string | null;
}

export interface PrincipalSummary {
  id: string;
  displayName: string;
  kind: PrincipalKind;
}

export interface IssueRequestSummary {
  id: string;
  createdAt: string;
  title: string;
  status: RequestStatus;
  requestedBy: PrincipalSummary;
}

export interface PullRequestSummary {
  id: string;
  createdAt: string;
  headBranch: string;
  baseBranch: string;
  title: string;
  status: RequestStatus;
  requestedBy: PrincipalSummary;
}

export interface DeploymentRequestSummary {
  id: string;
  createdAt: string;
  commitSha: string;
  environmentTier: EnvironmentTier;
  status: RequestStatus;
  requestedBy: PrincipalSummary;
  approvedBy: PrincipalSummary | null;
}

export interface SchemaMigrationSummary {
  id: string;
  createdAt: string;
  migrationName: string;
  migrationSha: string;
  status: RequestStatus;
  requestedBy: PrincipalSummary;
  approvedBy: PrincipalSummary | null;
}

export interface ExecutionSummary {
  id: string;
  startedAt: string;
  requestId: string;
  requestKind: RequestKind;
  status: ExecutionStatus;
  externalReference: string | null;
  failureMessage: string | null;
}

export interface RequestIssueCreationInput {
  delegation: string;
  repository: string;
  connector: string;
  title: string;
  body: string;
}

export interface RequestPullRequestInput {
  delegation: string;
  repository: string;
  connector: string;
  headBranch: string;
  baseBranch: string;
  title: string;
}

export interface RequestStagingDeploymentInput {
  delegation: string;
  environment: string;
  connector: string;
  commitSha: string;
}

export interface RequestProductionDeploymentInput {
  delegation: string;
  environment: string;
  connector: string;
  commitSha: string;
}

export interface RequestSchemaMigrationInput {
  delegation: string;
  environment: string;
  connector: string;
  migrationName: string;
  migrationSha: string;
}

export interface ApproveProductionDeploymentInput {
  request: string;
}

export interface RejectProductionDeploymentInput {
  request: string;
}

export interface ApproveSchemaMigrationInput {
  request: string;
}

export interface RejectSchemaMigrationInput {
  request: string;
}

export interface DispatchIssueCreationInput {
  request: string;
  allowance: string;
  connector: string;
}

export interface DispatchPullRequestInput {
  request: string;
  allowance: string;
  connector: string;
}

export interface DispatchStagingDeploymentInput {
  request: string;
  allowance: string;
  connector: string;
}

export interface DispatchApprovedDeploymentInput {
  request: string;
  allowance: string;
  connector: string;
}

export interface DispatchApprovedSchemaMigrationInput {
  request: string;
  allowance: string;
  connector: string;
}

export interface CompleteExecutionInput {
  execution: string;
  externalReference: string;
}

export interface FailExecutionInput {
  execution: string;
  failureMessage: string;
}

export interface MyIssueRequestsInput {
  cursor?: string;
}

export interface MyPullRequestsInput {
  cursor?: string;
}

export interface MyDeploymentRequestsInput {
  cursor?: string;
}

export interface DeploymentApprovalInboxInput {
  cursor?: string;
}

export interface MySchemaMigrationRequestsInput {
  cursor?: string;
}

export interface MigrationApprovalInboxInput {
  cursor?: string;
}

export interface MyExecutionsInput {
  cursor?: string;
}

