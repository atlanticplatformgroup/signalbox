// Generated by ModelLang. Do not edit.
import type { Organization, Principal, AgentCredentialMetadata, Connector, Repository, Environment, Delegation, Allowance, IssueRequest, PullRequest, DeploymentRequest, SchemaMigrationRequest, Approval, Execution, PrincipalSummary, IssueRequestSummary, PullRequestSummary, DeploymentRequestSummary, SchemaMigrationSummary, ExecutionSummary, RequestIssueCreationInput, RequestPullRequestInput, RequestStagingDeploymentInput, RequestProductionDeploymentInput, RequestSchemaMigrationInput, ApproveProductionDeploymentInput, RejectProductionDeploymentInput, ApproveSchemaMigrationInput, RejectSchemaMigrationInput, DispatchIssueCreationInput, DispatchPullRequestInput, DispatchStagingDeploymentInput, DispatchApprovedDeploymentInput, DispatchApprovedSchemaMigrationInput, CompleteExecutionInput, FailExecutionInput, MyIssueRequestsInput, MyPullRequestsInput, MyDeploymentRequestsInput, DeploymentApprovalInboxInput, MySchemaMigrationRequestsInput, MigrationApprovalInboxInput, MyExecutionsInput, ApplicabilityDecision, ApplicabilityOptions, ExecutionOptions, CursorPage } from "./types.js";
import { ValidationError, mapDatabaseError } from "./errors.js";

export interface QueryAdapter {
  query<Row = unknown>(text: string, values?: unknown[]): Promise<{ rows: Row[] }>;
}


const commandMetadataPattern = /^[A-Za-z0-9][A-Za-z0-9._:/-]{0,127}$/;
function executionMetadata(options: ExecutionOptions, required: boolean, ruleId: string): {
  idempotencyKey: string;
  correlationId: string;
  causationId: string;
} {
  const key = options.idempotencyKey ?? "";
  if (required && !key) throw new ValidationError("An idempotency key is required", "ML_IDEMPOTENCY_REQUIRED", ruleId);
  if (!required && key) throw new ValidationError("This action does not support idempotency keys", "ML_IDEMPOTENCY_UNSUPPORTED", ruleId);
  const correlation = options.correlationId ?? (required ? key : "");
  const causation = options.causationId ?? "";
  if ((key && !commandMetadataPattern.test(key))
    || (correlation && !commandMetadataPattern.test(correlation))
    || (causation && !commandMetadataPattern.test(causation))) {
    throw new ValidationError("Command metadata is invalid", "ML_VALIDATION", ruleId);
  }
  return { idempotencyKey: key, correlationId: correlation, causationId: causation };
}


export class SignalboxClient {
  constructor(private readonly adapter: QueryAdapter) {}

  async requestIssueCreation(input: RequestIssueCreationInput, options: ExecutionOptions = {}): Promise<IssueRequest> {
    try {
      const metadata = executionMetadata(options, true, "idempotency:action:act_ea693a4d658449fbab5741b8369bc276");
      const result = await this.adapter.query<{ value: IssueRequest }>(
        "WITH execution_context AS MATERIALIZED (SELECT pg_catalog.set_config('modellang.expected_revision', $6, true), pg_catalog.set_config('modellang.idempotency_key', $7, true), pg_catalog.set_config('modellang.correlation_id', $8, true), pg_catalog.set_config('modellang.causation_id', $9, true)) SELECT \"model_signalbox\".\"request_issue_creation\"($1, $2, $3, $4, $5) AS value FROM execution_context",
        [input.delegation, input.repository, input.connector, input.title, input.body, options.expectedRevision ?? "", metadata.idempotencyKey, metadata.correlationId, metadata.causationId],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
}

  async requestPullRequest(input: RequestPullRequestInput, options: ExecutionOptions = {}): Promise<PullRequest> {
    try {
      const metadata = executionMetadata(options, true, "idempotency:action:act_c4bb8af190dd48efb9784efb9ff9030c");
      const result = await this.adapter.query<{ value: PullRequest }>(
        "WITH execution_context AS MATERIALIZED (SELECT pg_catalog.set_config('modellang.expected_revision', $7, true), pg_catalog.set_config('modellang.idempotency_key', $8, true), pg_catalog.set_config('modellang.correlation_id', $9, true), pg_catalog.set_config('modellang.causation_id', $10, true)) SELECT \"model_signalbox\".\"request_pull_request\"($1, $2, $3, $4, $5, $6) AS value FROM execution_context",
        [input.delegation, input.repository, input.connector, input.headBranch, input.baseBranch, input.title, options.expectedRevision ?? "", metadata.idempotencyKey, metadata.correlationId, metadata.causationId],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
}

  async requestStagingDeployment(input: RequestStagingDeploymentInput, options: ExecutionOptions = {}): Promise<DeploymentRequest> {
    try {
      const metadata = executionMetadata(options, true, "idempotency:action:act_1388eb9f38684fa0830f60156cdba497");
      const result = await this.adapter.query<{ value: DeploymentRequest }>(
        "WITH execution_context AS MATERIALIZED (SELECT pg_catalog.set_config('modellang.expected_revision', $5, true), pg_catalog.set_config('modellang.idempotency_key', $6, true), pg_catalog.set_config('modellang.correlation_id', $7, true), pg_catalog.set_config('modellang.causation_id', $8, true)) SELECT \"model_signalbox\".\"request_staging_deployment\"($1, $2, $3, $4) AS value FROM execution_context",
        [input.delegation, input.environment, input.connector, input.commitSha, options.expectedRevision ?? "", metadata.idempotencyKey, metadata.correlationId, metadata.causationId],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
}

  async requestProductionDeployment(input: RequestProductionDeploymentInput, options: ExecutionOptions = {}): Promise<DeploymentRequest> {
    try {
      const metadata = executionMetadata(options, true, "idempotency:action:act_d10d1618ed4045f396b64fc3745ce3dd");
      const result = await this.adapter.query<{ value: DeploymentRequest }>(
        "WITH execution_context AS MATERIALIZED (SELECT pg_catalog.set_config('modellang.expected_revision', $5, true), pg_catalog.set_config('modellang.idempotency_key', $6, true), pg_catalog.set_config('modellang.correlation_id', $7, true), pg_catalog.set_config('modellang.causation_id', $8, true)) SELECT \"model_signalbox\".\"request_production_deployment\"($1, $2, $3, $4) AS value FROM execution_context",
        [input.delegation, input.environment, input.connector, input.commitSha, options.expectedRevision ?? "", metadata.idempotencyKey, metadata.correlationId, metadata.causationId],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
}

  async requestSchemaMigration(input: RequestSchemaMigrationInput, options: ExecutionOptions = {}): Promise<SchemaMigrationRequest> {
    try {
      const metadata = executionMetadata(options, true, "idempotency:action:act_411bfff32560406186bd2d442f1ecf3b");
      const result = await this.adapter.query<{ value: SchemaMigrationRequest }>(
        "WITH execution_context AS MATERIALIZED (SELECT pg_catalog.set_config('modellang.expected_revision', $6, true), pg_catalog.set_config('modellang.idempotency_key', $7, true), pg_catalog.set_config('modellang.correlation_id', $8, true), pg_catalog.set_config('modellang.causation_id', $9, true)) SELECT \"model_signalbox\".\"request_schema_migration\"($1, $2, $3, $4, $5) AS value FROM execution_context",
        [input.delegation, input.environment, input.connector, input.migrationName, input.migrationSha, options.expectedRevision ?? "", metadata.idempotencyKey, metadata.correlationId, metadata.causationId],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
}

  async approveProductionDeployment(input: ApproveProductionDeploymentInput, options: ExecutionOptions = {}): Promise<Approval> {
    try {
      const metadata = executionMetadata(options, true, "idempotency:action:act_047a601f15384b5ea4bfa05b5ef72676");
      const result = await this.adapter.query<{ value: Approval }>(
        "WITH execution_context AS MATERIALIZED (SELECT pg_catalog.set_config('modellang.expected_revision', $2, true), pg_catalog.set_config('modellang.idempotency_key', $3, true), pg_catalog.set_config('modellang.correlation_id', $4, true), pg_catalog.set_config('modellang.causation_id', $5, true)) SELECT \"model_signalbox\".\"approve_production_deployment\"($1) AS value FROM execution_context",
        [input.request, options.expectedRevision ?? "", metadata.idempotencyKey, metadata.correlationId, metadata.causationId],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
}

  async rejectProductionDeployment(input: RejectProductionDeploymentInput, options: ExecutionOptions = {}): Promise<DeploymentRequest> {
    try {
      const metadata = executionMetadata(options, true, "idempotency:action:act_18ab026d358144dfa4d1729e40dd832e");
      const result = await this.adapter.query<{ value: DeploymentRequest }>(
        "WITH execution_context AS MATERIALIZED (SELECT pg_catalog.set_config('modellang.expected_revision', $2, true), pg_catalog.set_config('modellang.idempotency_key', $3, true), pg_catalog.set_config('modellang.correlation_id', $4, true), pg_catalog.set_config('modellang.causation_id', $5, true)) SELECT \"model_signalbox\".\"reject_production_deployment\"($1) AS value FROM execution_context",
        [input.request, options.expectedRevision ?? "", metadata.idempotencyKey, metadata.correlationId, metadata.causationId],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
}

  async approveSchemaMigration(input: ApproveSchemaMigrationInput, options: ExecutionOptions = {}): Promise<Approval> {
    try {
      const metadata = executionMetadata(options, true, "idempotency:action:act_4c170dfcb0224cb8aaf078fe6b6ef23d");
      const result = await this.adapter.query<{ value: Approval }>(
        "WITH execution_context AS MATERIALIZED (SELECT pg_catalog.set_config('modellang.expected_revision', $2, true), pg_catalog.set_config('modellang.idempotency_key', $3, true), pg_catalog.set_config('modellang.correlation_id', $4, true), pg_catalog.set_config('modellang.causation_id', $5, true)) SELECT \"model_signalbox\".\"approve_schema_migration\"($1) AS value FROM execution_context",
        [input.request, options.expectedRevision ?? "", metadata.idempotencyKey, metadata.correlationId, metadata.causationId],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
}

  async rejectSchemaMigration(input: RejectSchemaMigrationInput, options: ExecutionOptions = {}): Promise<SchemaMigrationRequest> {
    try {
      const metadata = executionMetadata(options, true, "idempotency:action:act_d3a1935e42f24e4d84d25bc05ee690ad");
      const result = await this.adapter.query<{ value: SchemaMigrationRequest }>(
        "WITH execution_context AS MATERIALIZED (SELECT pg_catalog.set_config('modellang.expected_revision', $2, true), pg_catalog.set_config('modellang.idempotency_key', $3, true), pg_catalog.set_config('modellang.correlation_id', $4, true), pg_catalog.set_config('modellang.causation_id', $5, true)) SELECT \"model_signalbox\".\"reject_schema_migration\"($1) AS value FROM execution_context",
        [input.request, options.expectedRevision ?? "", metadata.idempotencyKey, metadata.correlationId, metadata.causationId],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
}

  async dispatchIssueCreation(input: DispatchIssueCreationInput, options: ExecutionOptions = {}): Promise<Execution> {
    try {
      const metadata = executionMetadata(options, true, "idempotency:action:act_cbb72fd307704ab3927aa4bea8112fbf");
      const result = await this.adapter.query<{ value: Execution }>(
        "WITH execution_context AS MATERIALIZED (SELECT pg_catalog.set_config('modellang.expected_revision', $4, true), pg_catalog.set_config('modellang.idempotency_key', $5, true), pg_catalog.set_config('modellang.correlation_id', $6, true), pg_catalog.set_config('modellang.causation_id', $7, true)) SELECT \"model_signalbox\".\"dispatch_issue_creation\"($1, $2, $3) AS value FROM execution_context",
        [input.request, input.allowance, input.connector, options.expectedRevision ?? "", metadata.idempotencyKey, metadata.correlationId, metadata.causationId],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
}

  async dispatchPullRequest(input: DispatchPullRequestInput, options: ExecutionOptions = {}): Promise<Execution> {
    try {
      const metadata = executionMetadata(options, true, "idempotency:action:act_3e99da927be642efac3d1bee026ef00a");
      const result = await this.adapter.query<{ value: Execution }>(
        "WITH execution_context AS MATERIALIZED (SELECT pg_catalog.set_config('modellang.expected_revision', $4, true), pg_catalog.set_config('modellang.idempotency_key', $5, true), pg_catalog.set_config('modellang.correlation_id', $6, true), pg_catalog.set_config('modellang.causation_id', $7, true)) SELECT \"model_signalbox\".\"dispatch_pull_request\"($1, $2, $3) AS value FROM execution_context",
        [input.request, input.allowance, input.connector, options.expectedRevision ?? "", metadata.idempotencyKey, metadata.correlationId, metadata.causationId],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
}

  async dispatchStagingDeployment(input: DispatchStagingDeploymentInput, options: ExecutionOptions = {}): Promise<Execution> {
    try {
      const metadata = executionMetadata(options, true, "idempotency:action:act_3e26a4d454634bf3a2058204146d7c45");
      const result = await this.adapter.query<{ value: Execution }>(
        "WITH execution_context AS MATERIALIZED (SELECT pg_catalog.set_config('modellang.expected_revision', $4, true), pg_catalog.set_config('modellang.idempotency_key', $5, true), pg_catalog.set_config('modellang.correlation_id', $6, true), pg_catalog.set_config('modellang.causation_id', $7, true)) SELECT \"model_signalbox\".\"dispatch_staging_deployment\"($1, $2, $3) AS value FROM execution_context",
        [input.request, input.allowance, input.connector, options.expectedRevision ?? "", metadata.idempotencyKey, metadata.correlationId, metadata.causationId],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
}

  async dispatchApprovedDeployment(input: DispatchApprovedDeploymentInput, options: ExecutionOptions = {}): Promise<Execution> {
    try {
      const metadata = executionMetadata(options, true, "idempotency:action:act_4a9421bfc2e744969b9f73109e6cda54");
      const result = await this.adapter.query<{ value: Execution }>(
        "WITH execution_context AS MATERIALIZED (SELECT pg_catalog.set_config('modellang.expected_revision', $4, true), pg_catalog.set_config('modellang.idempotency_key', $5, true), pg_catalog.set_config('modellang.correlation_id', $6, true), pg_catalog.set_config('modellang.causation_id', $7, true)) SELECT \"model_signalbox\".\"dispatch_approved_deployment\"($1, $2, $3) AS value FROM execution_context",
        [input.request, input.allowance, input.connector, options.expectedRevision ?? "", metadata.idempotencyKey, metadata.correlationId, metadata.causationId],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
}

  async dispatchApprovedSchemaMigration(input: DispatchApprovedSchemaMigrationInput, options: ExecutionOptions = {}): Promise<Execution> {
    try {
      const metadata = executionMetadata(options, true, "idempotency:action:act_70d3862584094631aca61e9db664d991");
      const result = await this.adapter.query<{ value: Execution }>(
        "WITH execution_context AS MATERIALIZED (SELECT pg_catalog.set_config('modellang.expected_revision', $4, true), pg_catalog.set_config('modellang.idempotency_key', $5, true), pg_catalog.set_config('modellang.correlation_id', $6, true), pg_catalog.set_config('modellang.causation_id', $7, true)) SELECT \"model_signalbox\".\"dispatch_approved_schema_migration\"($1, $2, $3) AS value FROM execution_context",
        [input.request, input.allowance, input.connector, options.expectedRevision ?? "", metadata.idempotencyKey, metadata.correlationId, metadata.causationId],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
}

  async completeExecution(input: CompleteExecutionInput, options: ExecutionOptions = {}): Promise<Execution> {
    try {
      const metadata = executionMetadata(options, true, "idempotency:action:act_5be24324b68d4c2eb334732b36e1b16c");
      const result = await this.adapter.query<{ value: Execution }>(
        "WITH execution_context AS MATERIALIZED (SELECT pg_catalog.set_config('modellang.expected_revision', $3, true), pg_catalog.set_config('modellang.idempotency_key', $4, true), pg_catalog.set_config('modellang.correlation_id', $5, true), pg_catalog.set_config('modellang.causation_id', $6, true)) SELECT \"model_signalbox\".\"complete_execution\"($1, $2) AS value FROM execution_context",
        [input.execution, input.externalReference, options.expectedRevision ?? "", metadata.idempotencyKey, metadata.correlationId, metadata.causationId],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
}

  async failExecution(input: FailExecutionInput, options: ExecutionOptions = {}): Promise<Execution> {
    try {
      const metadata = executionMetadata(options, true, "idempotency:action:act_926686163a6544e79d44dea9336d2c88");
      const result = await this.adapter.query<{ value: Execution }>(
        "WITH execution_context AS MATERIALIZED (SELECT pg_catalog.set_config('modellang.expected_revision', $3, true), pg_catalog.set_config('modellang.idempotency_key', $4, true), pg_catalog.set_config('modellang.correlation_id', $5, true), pg_catalog.set_config('modellang.causation_id', $6, true)) SELECT \"model_signalbox\".\"fail_execution\"($1, $2) AS value FROM execution_context",
        [input.execution, input.failureMessage, options.expectedRevision ?? "", metadata.idempotencyKey, metadata.correlationId, metadata.causationId],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
}

  async assessRequestIssueCreation(input: RequestIssueCreationInput, options: ApplicabilityOptions = {}): Promise<ApplicabilityDecision> {
    try {
      const result = await this.adapter.query<{ value: ApplicabilityDecision }>(
        'SELECT "model_signalbox"."decide_act_ea693a4d658449fbab5741b8369bc276"($1, $2, $3, $4, $5, $6) AS value',
        [input.delegation, input.repository, input.connector, input.title, input.body, options.expectedRevision ?? null],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
  }

  async assessRequestPullRequest(input: RequestPullRequestInput, options: ApplicabilityOptions = {}): Promise<ApplicabilityDecision> {
    try {
      const result = await this.adapter.query<{ value: ApplicabilityDecision }>(
        'SELECT "model_signalbox"."decide_act_c4bb8af190dd48efb9784efb9ff9030c"($1, $2, $3, $4, $5, $6, $7) AS value',
        [input.delegation, input.repository, input.connector, input.headBranch, input.baseBranch, input.title, options.expectedRevision ?? null],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
  }

  async assessRequestStagingDeployment(input: RequestStagingDeploymentInput, options: ApplicabilityOptions = {}): Promise<ApplicabilityDecision> {
    try {
      const result = await this.adapter.query<{ value: ApplicabilityDecision }>(
        'SELECT "model_signalbox"."decide_act_1388eb9f38684fa0830f60156cdba497"($1, $2, $3, $4, $5) AS value',
        [input.delegation, input.environment, input.connector, input.commitSha, options.expectedRevision ?? null],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
  }

  async assessRequestProductionDeployment(input: RequestProductionDeploymentInput, options: ApplicabilityOptions = {}): Promise<ApplicabilityDecision> {
    try {
      const result = await this.adapter.query<{ value: ApplicabilityDecision }>(
        'SELECT "model_signalbox"."decide_act_d10d1618ed4045f396b64fc3745ce3dd"($1, $2, $3, $4, $5) AS value',
        [input.delegation, input.environment, input.connector, input.commitSha, options.expectedRevision ?? null],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
  }

  async assessRequestSchemaMigration(input: RequestSchemaMigrationInput, options: ApplicabilityOptions = {}): Promise<ApplicabilityDecision> {
    try {
      const result = await this.adapter.query<{ value: ApplicabilityDecision }>(
        'SELECT "model_signalbox"."decide_act_411bfff32560406186bd2d442f1ecf3b"($1, $2, $3, $4, $5, $6) AS value',
        [input.delegation, input.environment, input.connector, input.migrationName, input.migrationSha, options.expectedRevision ?? null],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
  }

  async assessApproveProductionDeployment(input: ApproveProductionDeploymentInput, options: ApplicabilityOptions = {}): Promise<ApplicabilityDecision> {
    try {
      const result = await this.adapter.query<{ value: ApplicabilityDecision }>(
        'SELECT "model_signalbox"."decide_act_047a601f15384b5ea4bfa05b5ef72676"($1, $2) AS value',
        [input.request, options.expectedRevision ?? null],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
  }

  async assessRejectProductionDeployment(input: RejectProductionDeploymentInput, options: ApplicabilityOptions = {}): Promise<ApplicabilityDecision> {
    try {
      const result = await this.adapter.query<{ value: ApplicabilityDecision }>(
        'SELECT "model_signalbox"."decide_act_18ab026d358144dfa4d1729e40dd832e"($1, $2) AS value',
        [input.request, options.expectedRevision ?? null],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
  }

  async assessApproveSchemaMigration(input: ApproveSchemaMigrationInput, options: ApplicabilityOptions = {}): Promise<ApplicabilityDecision> {
    try {
      const result = await this.adapter.query<{ value: ApplicabilityDecision }>(
        'SELECT "model_signalbox"."decide_act_4c170dfcb0224cb8aaf078fe6b6ef23d"($1, $2) AS value',
        [input.request, options.expectedRevision ?? null],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
  }

  async assessRejectSchemaMigration(input: RejectSchemaMigrationInput, options: ApplicabilityOptions = {}): Promise<ApplicabilityDecision> {
    try {
      const result = await this.adapter.query<{ value: ApplicabilityDecision }>(
        'SELECT "model_signalbox"."decide_act_d3a1935e42f24e4d84d25bc05ee690ad"($1, $2) AS value',
        [input.request, options.expectedRevision ?? null],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
  }

  async assessDispatchIssueCreation(input: DispatchIssueCreationInput, options: ApplicabilityOptions = {}): Promise<ApplicabilityDecision> {
    try {
      const result = await this.adapter.query<{ value: ApplicabilityDecision }>(
        'SELECT "model_signalbox"."decide_act_cbb72fd307704ab3927aa4bea8112fbf"($1, $2, $3, $4) AS value',
        [input.request, input.allowance, input.connector, options.expectedRevision ?? null],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
  }

  async assessDispatchPullRequest(input: DispatchPullRequestInput, options: ApplicabilityOptions = {}): Promise<ApplicabilityDecision> {
    try {
      const result = await this.adapter.query<{ value: ApplicabilityDecision }>(
        'SELECT "model_signalbox"."decide_act_3e99da927be642efac3d1bee026ef00a"($1, $2, $3, $4) AS value',
        [input.request, input.allowance, input.connector, options.expectedRevision ?? null],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
  }

  async assessDispatchStagingDeployment(input: DispatchStagingDeploymentInput, options: ApplicabilityOptions = {}): Promise<ApplicabilityDecision> {
    try {
      const result = await this.adapter.query<{ value: ApplicabilityDecision }>(
        'SELECT "model_signalbox"."decide_act_3e26a4d454634bf3a2058204146d7c45"($1, $2, $3, $4) AS value',
        [input.request, input.allowance, input.connector, options.expectedRevision ?? null],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
  }

  async assessDispatchApprovedDeployment(input: DispatchApprovedDeploymentInput, options: ApplicabilityOptions = {}): Promise<ApplicabilityDecision> {
    try {
      const result = await this.adapter.query<{ value: ApplicabilityDecision }>(
        'SELECT "model_signalbox"."decide_act_4a9421bfc2e744969b9f73109e6cda54"($1, $2, $3, $4) AS value',
        [input.request, input.allowance, input.connector, options.expectedRevision ?? null],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
  }

  async assessDispatchApprovedSchemaMigration(input: DispatchApprovedSchemaMigrationInput, options: ApplicabilityOptions = {}): Promise<ApplicabilityDecision> {
    try {
      const result = await this.adapter.query<{ value: ApplicabilityDecision }>(
        'SELECT "model_signalbox"."decide_act_70d3862584094631aca61e9db664d991"($1, $2, $3, $4) AS value',
        [input.request, input.allowance, input.connector, options.expectedRevision ?? null],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
  }

  async assessCompleteExecution(input: CompleteExecutionInput, options: ApplicabilityOptions = {}): Promise<ApplicabilityDecision> {
    try {
      const result = await this.adapter.query<{ value: ApplicabilityDecision }>(
        'SELECT "model_signalbox"."decide_act_5be24324b68d4c2eb334732b36e1b16c"($1, $2, $3) AS value',
        [input.execution, input.externalReference, options.expectedRevision ?? null],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
  }

  async assessFailExecution(input: FailExecutionInput, options: ApplicabilityOptions = {}): Promise<ApplicabilityDecision> {
    try {
      const result = await this.adapter.query<{ value: ApplicabilityDecision }>(
        'SELECT "model_signalbox"."decide_act_926686163a6544e79d44dea9336d2c88"($1, $2, $3) AS value',
        [input.execution, input.failureMessage, options.expectedRevision ?? null],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
  }

  async myIssueRequests(input: MyIssueRequestsInput): Promise<CursorPage<IssueRequestSummary>> {
    try {
      const result = await this.adapter.query<{ value: CursorPage<IssueRequestSummary> }>(
        'SELECT "model_signalbox"."my_issue_requests"($1) AS value',
        [input.cursor ?? null],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
  }

  async myPullRequests(input: MyPullRequestsInput): Promise<CursorPage<PullRequestSummary>> {
    try {
      const result = await this.adapter.query<{ value: CursorPage<PullRequestSummary> }>(
        'SELECT "model_signalbox"."my_pull_requests"($1) AS value',
        [input.cursor ?? null],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
  }

  async myDeploymentRequests(input: MyDeploymentRequestsInput): Promise<CursorPage<DeploymentRequestSummary>> {
    try {
      const result = await this.adapter.query<{ value: CursorPage<DeploymentRequestSummary> }>(
        'SELECT "model_signalbox"."my_deployment_requests"($1) AS value',
        [input.cursor ?? null],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
  }

  async deploymentApprovalInbox(input: DeploymentApprovalInboxInput): Promise<CursorPage<DeploymentRequestSummary>> {
    try {
      const result = await this.adapter.query<{ value: CursorPage<DeploymentRequestSummary> }>(
        'SELECT "model_signalbox"."deployment_approval_inbox"($1) AS value',
        [input.cursor ?? null],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
  }

  async mySchemaMigrationRequests(input: MySchemaMigrationRequestsInput): Promise<CursorPage<SchemaMigrationSummary>> {
    try {
      const result = await this.adapter.query<{ value: CursorPage<SchemaMigrationSummary> }>(
        'SELECT "model_signalbox"."my_schema_migration_requests"($1) AS value',
        [input.cursor ?? null],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
  }

  async migrationApprovalInbox(input: MigrationApprovalInboxInput): Promise<CursorPage<SchemaMigrationSummary>> {
    try {
      const result = await this.adapter.query<{ value: CursorPage<SchemaMigrationSummary> }>(
        'SELECT "model_signalbox"."migration_approval_inbox"($1) AS value',
        [input.cursor ?? null],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
  }

  async myExecutions(input: MyExecutionsInput): Promise<CursorPage<ExecutionSummary>> {
    try {
      const result = await this.adapter.query<{ value: CursorPage<ExecutionSummary> }>(
        'SELECT "model_signalbox"."my_executions"($1) AS value',
        [input.cursor ?? null],
      );
      return result.rows[0]!.value;
    } catch (error) {
      throw mapDatabaseError(error);
    }
  }
}
